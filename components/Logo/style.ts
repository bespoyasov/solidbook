import styled from 'styled-components'

export default styled.div`
  font-size: 2.3rem;
  font-weight: 700;

  a,
  a:visited {
    color: black;
    text-decoration: none;
  }

  a:hover {
    color: #fb3347;
  }
`
