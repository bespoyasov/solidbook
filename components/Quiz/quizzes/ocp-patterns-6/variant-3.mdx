3.
```ts
class GreetingWithoutSpaces implements GreetingDecorator {
  decorated: Greeting

  constructor(decorated: Greeting) {
    this.decorated = decorated
  }

  greet(): string {
    return this.decorated.greet().replace(/\s/g, '')
  }
}
```
