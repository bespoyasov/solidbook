На первый взгляд всё в порядке, но если сам интерфейс `AreaCalculatable` изменится, класс `Square` перестанет ему соответствовать, а без директивы `implements` компилятор не сможет об этом предупредить
