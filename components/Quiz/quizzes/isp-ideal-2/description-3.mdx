Абстрактный класс `Programmer` реализует интерфейс `SodaConsumer`, который содержит метод `drinkSoda`. Программисты класса `OnDietProgrammer` не пьют газировку, поэтому этот метод им не нужен, но они должны его реализовать из-за интерфейса родительского класса
